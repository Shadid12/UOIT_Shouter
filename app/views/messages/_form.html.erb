<%= simple_form_for @message do |f| %>
	<%= f.input :title, label: "Titles can be cool" %>
	<%= f.input :description %>
	<%= f.input :avatar, label: 'Add a pic', as: :file %>
	<%= f.input :tag_list, label: "Tag/" %>
	<%= f.button :submit, 'Shout now!!!' %>
	
<% end %>


		

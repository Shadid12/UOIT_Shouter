<%= simple_form_for([@message, @message.comments.build]) do |f| %>
	<%= f.input :content, label: "shout back yo" %> 
	<%= f.button :submit, 'shout_back', :class => 'btn-custom' %>
<% end %>

<small>refresh page to comment with facebook</small>